export enum StatusTypes {
  draft = 'draft',
  active = 'active',
  removal = 'removal',
  archive = 'archive',
  copiyed = 'copiyed',
}

export enum StatusEnum {
  draft = 'Черновик',
  active = 'Активный',
  removal = 'На удаление',
  archive = 'Архивный',
  copiyed = 'Скопированный',
}

// for add homework
export enum AddStatusEnum {
  draft = 'Черновик',
}

// for edit homework
export enum EditStatusEnum {
  draft = 'Черновик',
  active = 'Активный',
}
