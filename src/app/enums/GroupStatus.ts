export enum GroupStatus {
  active = 'активная',
  complete = 'завершённая',
  archive = 'архивная',
}

export enum GroupStatusValue {
  active = 'active',
  complete = 'complete',
  archive = 'archive',
}
